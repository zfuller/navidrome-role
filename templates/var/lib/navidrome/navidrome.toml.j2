#jinja2: trim_blocks:False
# {{ ansible_managed }}
{% for key, value in navidrome_config.items() -%}
{% if value is defined and (value is number or value is boolean) %}{{ key }} = {{ value | lower }}
{% elif value is defined and value %}{{ key }} = "{{ value }}"
{% endif %}
{%- endfor %}
